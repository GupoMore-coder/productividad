# Archivo Histórico

Este directorio contiene SQLs de migraciones antiguas que fueron reemplazadas por las migraciones individuales en `supabase/migrations/`.

**NO USAR** estos archivos directamente. Todas las migraciones activas están en `supabase/migrations/` y deben aplicarse en orden cronológico.
