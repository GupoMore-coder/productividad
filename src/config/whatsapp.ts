// WHATSAPP_CONFIG - Solo para el frontend de WhatsApp Editor Modal
// Los tokens de WhatsApp API se configuran como Secrets en Supabase Edge Functions.
// El metodo de envio OFICIAL es via Edge Function (whatsapp-automation).
// Este archivo solo proporciona la version de API para uso en el Editor Modal.

export const WHATSAPP_CONFIG = {
  VERSION: 'v21.0',
};
